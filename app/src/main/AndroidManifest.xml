<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AptekaMini"
        tools:targetApi="31">
        <activity
            android:name=".InfoViewActivity"
            android:exported="false" />
        <activity
            android:name=".PharmacyView"
            android:exported="false" />
        <activity
            android:name=".SearchViewActivity"
            android:exported="false" />
        <activity
            android:name=".SearchView"
            android:exported="false"
            android:label="@string/title_activity_search_view"
            android:theme="@style/Theme.AptekaMini.NoActionBar" />
        <activity
            android:name=".activity_search_view"
            android:exported="false"
            android:label="@string/title_activity_search_view"
            android:theme="@style/Theme.AptekaMini" />
        <activity
            android:name=".MedicActivity"
            android:exported="false" />
        <activity
            android:name=".MedicomentsView"
            android:exported="false"
            android:label="@string/title_activity_medicoments_view"
            android:theme="@style/Theme.AptekaMini" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>